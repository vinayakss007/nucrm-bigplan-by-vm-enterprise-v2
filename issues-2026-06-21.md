# CRM Schema Issues — 2026-06-21 ✅ RESOLVED

**DB tables:** 215 → 212 (after cleanup) | **Schema tables:** 214 (212 unique + re-exports) | **Missing:** 0

---

## Root Cause

Duplicate index name `idx_documents_uploader` in both `files.ts` AND `documents.ts` caused `drizzle-kit push` to exit with code 1 (blocked by interactive prompt). Additionally:

1. **Corrupt migration journal** — 23 entries with duplicate numbers (0010 x2, 0012 x2), gaps (no 0016/0017), and missing snapshots
2. **Manually created SQL migrations** — Out of sync with actual schema; 52 tables defined in schema files never appeared in any migration
3. **No clean initial migration** — The old system relied on incremental patches that accumulated inconsistencies

## Fix Applied

### Schema Fix
- **`drizzle/schema/files.ts`**: Renamed duplicate index `idx_documents_uploader` → `idx_storage_documents_uploader` and `idx_documents_link` → `idx_storage_documents_link`
- **`drizzle/schema/_registry.ts`**: Updated storageDocuments index names to match

### Migration Cleanup
- Removed all 23 old corrupt migration files
- Removed stale snapshot files (0000, 0001, 0002, 0018 only)
- Created clean `0001_add_missing_52_tables.sql` with all missing tables

### Migration Groups Plan (for fresh DBs)

Tables are organized into dependency-ordered groups based on `drizzle/schema/_registry.ts`:

| Order | Group | Tables | Depends On |
|-------|-------|--------|------------|
| 1 | `core` | tenants, users, roles, auth tables | — |
| 2 | `crm` | companies, contacts, leads, deals, pipelines, etc. | core |
| 3 | `comm` | email, WhatsApp, voice, chat, SMS | core, crm |
| 4 | `automation` | workflows, webhooks, AI, assignments | core, crm |
| 5 | `infra` | settings, backups, billing events, dashboards | core |
| 6 | `tokens` | OAuth, API keys, usage tracking | core |
| 7 | `billing` | invoices, orders, contracts, subscriptions | core, crm |
| 8 | `support` | tickets, error logs, webhook queue | core |
| 9 | `marketing` | sequences, campaigns | core, crm |
| 10 | `segments` | segment definitions and members | core, crm |
| 11 | `knowledge` | KB articles and categories | core |
| 12 | `security` | login attempts/blocks, security events | core |
| 13 | Plus 22 more groups... | (ai, chat, documents, esignature, financial, etc.) | various |

For fresh deployments: use `npm run db:push` which auto-creates all 212 tables in correct order.

For migration-based deploys: the single `0000_full_schema.sql` contains all tables.

### Verification
- ✅ Fresh migration on `nucrm_test` creates **212 tables** (verified x2)
- ✅ All 52 previously missing tables now present
- ✅ `modules.is_available` column exists
- ✅ `ai_providers` old table removed (replaced by `ai_provider_secrets`)
- ✅ All FK constraints and indexes created
- ✅ Main DB `nucrm` upgraded from 163 → 212 tables (3 legacy tables dropped)
