# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage
/test-results

# Next.js
/.next/
/.next-build/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files — blanket block all .env* except .env.example
.env*
!.env.example

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# SQLite (if used)
*.db
*.db-journal

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Project specific
/logs/
/cache/
*.log
npm-startup.log
app-startup.log
ngrok.log
ngrok.out

# Build artifacts
.next-build/
bundle-report.json

# Sandbox scratch output (screenshots, traces). Curated captures live in docs/.
.kiro/artifacts/
# Postgres server TLS material — generated per-deploy (O2), never committed.
server.key
server.crt
deploy/postgres/server.crt
deploy/postgres/server.key


# Test / simulation scratch artifacts — keep the repo root clean.
# Curated screenshots belong in docs/; throwaway captures do not.
*-test.png
tmp/*
!tmp/.gitkeep
tmp_*.sql
*.log.[0-9]
# Output of scripts/test_all_pages.sh — a generated report, not source.
PAGE_TEST_RESULTS.txt
